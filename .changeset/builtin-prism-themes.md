---
'bd-hub': minor
---

Built-in themes now ship matching Prism syntax highlighting: `dracula` and `monokai` style code blocks with their own palettes instead of falling back to Catppuccin. A user-configured `prismTheme` still takes precedence.
